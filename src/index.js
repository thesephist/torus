const {
    renderJDOM,
    Component,
    Styled,
    StyledComponent,
    List,
    ListOf,
    Record,
    Store,
    StoreOf,
    Router,
} = require('../dist/torus.no-debug.js');

const {
    jdom,
} = require('./jdom.js');

module.exports = {
    renderJDOM,
    Component,
    Styled,
    StyledComponent,
    List,
    ListOf,
    Record,
    Store,
    StoreOf,
    Router,
    jdom,
}
