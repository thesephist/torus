const {
    render,
    Component,
    Styled,
    StyledComponent,
    List,
    ListOf,
    Record,
    Store,
    StoreOf,
    Router,
} = require('../dist/torus.no-debug.js');

const {
    jdom,
    css,
} = require('./jdom.js');

module.exports = {
    render,
    Component,
    Styled,
    StyledComponent,
    List,
    ListOf,
    Record,
    Store,
    StoreOf,
    Router,
    jdom,
    css,
}
